name: SonarCloud

on:
  push:
    branches:
      - main
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: read

jobs:
  sonarcloud:
    name: SonarCloud
    uses: swissgrc/.github/.github/workflows/sonarcloud.yml@d91ad165227a6fecc01ede93fd6568bd32522455 # main
    secrets:
      gh-token: ${{ secrets.GITHUB_TOKEN }}
      sonar-token: ${{ secrets.SONAR_TOKEN }}
